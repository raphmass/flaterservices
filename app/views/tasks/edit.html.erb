<div class="container">
	<h1>Edit task <%= @task.id %> (only corresponding owner)</h1>
	<h2>current user <%= current_user.id %> / user id of taks <%= @task.user_id %></h2>

	<% if current_user.id == @task.user_id %>
	  <div class="container">
	  	<%= simple_form_for (@task) do |f| %>
	  	<%= f.error_notification %>
	  	<%= f.input :user_id, :as => :hidden, :input_html => { :value => current_user.id } %>
	  	<%= f.input :status, :as => :hidden, :input_html => { :value => 0 } %>
	  	<%= f.input :action, selected:'Visit', collection: Task::ACTIONS %>
	  	<%= f.input :location %>
	  	<%= f.input :date %>
	  	<%= f.input :price %>
	  	<%= f.button :submit, class: "btn-primary" %>
	  	<%= link_to "Back", my_tasks_path , {:class=>"btn btn-default"} %>
	  	<% end %>
	  </div>
	<% else %>
		<p>You cannot edit this task! Only the task owner can!</p>
	<% end %>



</div>